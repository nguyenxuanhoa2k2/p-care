import { ref } from "firebase/database"
import { database } from "../../utils/firebaseConfig"

const APPESP = () => {

   const dbRef = ref(database)
    return (
        <div style={{color:"black"}}>ng</div>
    )
}


export default APPESP